# Server-side Gemini key (required for real AI on mine/analyze/etc).
# Never prefix with VITE_ — that ships the key to the browser.
GEMINI_API_KEY=""

# Optional model overrides (defaults are fine for free-tier pilot).
GEMINI_MODEL_FAST="gemini-flash-lite-latest"
GEMINI_MODEL_REASONING="gemini-flash-lite-latest"

# Optional org label injected into AI prompts.
ORG_NAME="Finance Directorate"

# Shared catalogue store. Production VPS uses same-origin Postgres API:
VITE_SHEETS_ENDPOINT="/api/state"
# Legacy Google Apps Script (optional fallback): https://script.google.com/macros/s/.../exec

# Optional Neon remote-auth roll-up (OFF — catalogue lives in DATABASE_URL state table).
VITE_ENABLE_REMOTE_AUTH="false"
DATABASE_URL="postgresql://fin_blueprint:CHANGE_ME@127.0.0.1:5432/fin_blueprint"
AUTH_TOKEN_SECRET=""
PORT="3000"
# Shared secret for /api/state (server + baked into client as VITE_STATE_API_TOKEN at build).
STATE_API_TOKEN="change-me-long-random"
